import { styled } from '@mui/material/styles';

const JoinedChatListLayout = styled('div')(({ theme }) => ({
  height: '74%',
}));

function JoinedChatListOrganisms() {
  return <JoinedChatListLayout></JoinedChatListLayout>;
}

export default JoinedChatListOrganisms;
